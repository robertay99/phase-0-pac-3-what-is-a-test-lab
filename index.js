const name = "Susan";  // Update to match the test expectation
const height = 30;  // Update to be within the expected range (0 < height < 40)
const message = `${name} is ${height} inches tall`;

module.exports = {name, height, message};
